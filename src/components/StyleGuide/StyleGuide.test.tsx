import { describe, expect, test } from "vitest";

import StyleGuide from "./StyleGuide";

describe("StyleGuide", () => {
  test("exists", () => {
    expect(StyleGuide).not.toBe(undefined);
  });
});

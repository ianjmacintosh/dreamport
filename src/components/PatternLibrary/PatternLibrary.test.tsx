import { describe, expect, test } from "vitest";

import PatternLibrary from "./PatternLibrary";

describe("PatternLibrary", () => {
  test("exists", () => {
    expect(PatternLibrary).not.toBe(undefined);
  });
});

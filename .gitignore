# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TanStack Router
src/routeTree.gen.ts
.tanstack

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cloudflare Wrangler
.wrangler
.dev.vars*
!.dev.vars.example

# Vite `.env` files. Not used by this project — the one build-time value
# (VITE_TURNSTILE_SITE_KEY) is resolved from a committed per-CLOUDFLARE_ENV
# map in vite.config.ts. Ignored so a stray one can't quietly shadow it.
.env
.env.*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
